eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}

management:
  tracing:
    sampling:
      probability: 1.0
logging:
  file:
    name: C:\Users\Prasanna Kumar Killa\OneDrive - Sage IT, Inc\Desktop\logs\elk-stack.log
